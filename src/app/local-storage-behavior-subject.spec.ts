import { LocalStorageBehaviorSubject } from './local-storage-behavior-subject';

describe('LocalStorageBehaviorSubject', () => {
  it('should create an instance', () => {
    expect(new LocalStorageBehaviorSubject()).toBeTruthy();
  });
});
